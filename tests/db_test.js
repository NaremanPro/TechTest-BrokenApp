const test = require("tape");
const runDbBuild = require('../src/datasase/db_build');
const displayAnimals = require('../src/database/queries/animal_queries');

// test('testing displayAnimals query', t => {
//   runDbBuild((err, res) => {
//     t.error(err, 'No Error');
//     displayAnimals()
//   })
// })
